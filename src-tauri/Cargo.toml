[package]
name = "xiaojd-desktop"
version = "0.1.0"
description = "小决定 Desktop"
authors = ["小决定 Desktop"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
